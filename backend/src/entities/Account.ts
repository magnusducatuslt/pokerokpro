export type Account = {
    id: string,
    platform: string;
    status: string;
    isActive: string;
    accountName: string;
    game: number,
    balance: number,
    position:number,

    pubKey: string
}